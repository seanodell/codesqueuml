{
  "name": "codesqueuml",
  "version": "0.1.0",
  "dependencies": {
    "typescript": "*",
    "ts-node-dev": "*",
    "@types/node": "*",
    "shelljs": "*",
    "node-plantuml": "*"
  },
  "engines": {
    "node": ">=11.0.0"
  },
  "scripts": {
    "build": "tsc --declaration",
    "prepublish": "rm -rf output && rm -f *.d.ts && rm -f *.js && rm -f *.tgz && tsc --declaration",
    "run": "node_modules/ts-node-dev/bin/ts-node-dev test.ts"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/seanodell/codesqueuml.git"
  }
}
